import React from "react"
import { Link, Route } from "wouter"
import styled from "styled-components"

export const Footer = (props) => {
  return <StyledWrapper>{/* stuff */}</StyledWrapper>
}

const StyledWrapper = styled.div`
  display: flex;
  width: 100%;
  height: 64px;
  padding: 24px 48px;
`
