import * as React from "react"
import styled from "styled-components"

export const QuickViewLink = () => {
  return (
    <div className='quickViewLink'>
      <img src={window.pedersonsData.assets.searchIconWhiteUrl} alt='search icon' />
    </div>
  )
}
